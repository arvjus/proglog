'use strict';

describe('myApp.stats module', function() {

  beforeEach(module('myApp.stats'));

  describe('stats controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var statsCtrl = $controller('StatsCtrl');
      expect(statsCtrl).toBeDefined();
    }));

  });
});